# 汤仔助手 - 单人开发任务清单 (客户端优先)

> **开发模式**: 单人全栈开发  
> **总时长**: 6 个月 (24 周)  
> **策略**: 阶段 1 专注客户端 MVP (12周) → 阶段 2 开发管理端 (12周)

---

## 🎯 总体策略

### 为什么这样安排?

**客户端优先的理由**:
- ✅ 用户能用起来才是最重要的,管理功能可以先手动操作
- ✅ 客户端开发过程中可以验证数据结构和云函数设计
- ✅ 避免管理端开发完了,客户端需求变更导致返工

**管理端延后的临时方案**:
- 📝 前 3 个月通过 uniCloud Web 控制台直接操作数据库
- 📝 管理员手动添加/修改智能体、网页卡片、飞书卡片
- 📝 用户管理、城市管理等通过数据库操作

---

## 📅 阶段 1: 客户端 MVP 开发 (Week 1-12, 约 3 个月)

### Week 1-2: 公共基础设施 (P0 - 所有功能的前提)

#### 1-1. 扩展数据库字段 ✅ 必须完成
- [ ] 在 uniCloud Web 控制台打开 `uni-id-users` 集合
- [ ] 为现有用户补充字段 (可以批量更新):
  ```javascript
  // 示例数据
  {
    city: 1,                    // 城市ID
    city_name: "上海1",          // 城市名称
    department: "开发部",        // 部门
    is_admin: false,            // 是否管理员
    role: ["user"],             // 角色: user / city_admin / super_admin
    status: "active",           // 状态: active / disabled
    avatar: "",                 // 头像URL
    permissions: []             // 权限列表(可选)
  }
  ```
- [ ] 更新 `uni-id-users.schema.json`,添加上述字段定义
- [ ] 上传 Schema 到云端

#### 1-2. 开发统一权限中间件 ✅ 核心
- [ ] 创建 `uniCloud/cloudfunctions/common/permission.js`:
  ```javascript
  module.exports = {
    // 获取用户权限过滤条件
    async getUserFilter(userId) { ... },
    
    // 检查管理员权限
    async checkAdminPermission(userId) { ... },
    
    // 检查城市管理权限
    async canManageCity(userId, targetCityId) { ... }
  }
  ```
- [ ] 在现有云对象 (user/agent/web-card/feishu) 中引入权限过滤
- [ ] 测试权限过滤逻辑 (不同城市/部门用户只能看到授权数据)

#### 1-3. 优化用户认证云对象
- [ ] 在 `user/index.obj.js` 的 `login()` 中返回完整用户信息:
  ```javascript
  userInfo: {
    _id, username, mobile, email,
    city, city_name, department,    // 新增
    is_admin, role, avatar          // 新增
  }
  ```
- [ ] 前端登录后存储完整用户信息到 `uni.setStorageSync('userInfo', ...)`

#### 1-4. 补充数据库索引
- [ ] 为各集合添加索引 (通过 uniCloud Web 控制台):
  - `uni-id-users`: mobile(唯一), city+department+is_admin
  - `agents`: is_active+navigation_tab+cities+departments
  - `web-cards`: is_active+navigation_tab+cities+departments
  - `feishu-cards`: is_active+cities+departments

---

### Week 3-5: 工作台 + 底部导航

#### 3-1. 工作台页面 (pages/workbench/workbench.vue)
- [ ] 顶部用户信息卡片 (头像、姓名、城市、部门)
- [ ] 快捷入口网格:
  - 智能体 (跳转 teaching)
  - 服务 (跳转 service)
  - 学习中心 (跳转 learning)
  - 飞书数据 (如果有权限)
- [ ] 最近使用的智能体列表 (从 localStorage 读取)
- [ ] 数据统计卡片 (可选):
  - 本周对话次数
  - 常用服务卡片数量

**技术要点**:
- 使用 `uni.getStorageSync('userInfo')` 获取用户信息
- 根据 `navigation_tab` 分组展示快捷入口

#### 3-2. 优化底部 TabBar
- [ ] 确认 4 个 Tab 图标和文案:
  - 工作台 (workbench)
  - 智能体 (teaching)
  - 服务 (service)
  - 学习中心 (learning)
  - 我的 (mine)
- [ ] 根据用户权限动态显示/隐藏某些 Tab (可选)

---

### Week 6-8: 智能体页面 (核心功能)

#### 6-1. 智能体列表页 (pages/teaching/teaching.vue)
- [ ] 调用 `agent.list({ navigation_tab: '教研' })` 获取数据
- [ ] 使用权限过滤 (后端 `common/permission.js` 自动处理)
- [ ] 卡片展示:
  - 智能体图标 (icon_name + icon_color)
  - 标题 + 描述
  - 标签 (城市、部门范围)
- [ ] 点击卡片 → 跳转到聊天页 (携带 agent_id)
- [ ] 下拉刷新 + 上拉加载更多
- [ ] 搜索功能 (按名称/描述)

**优化云对象**:
```javascript
// agent/index.obj.js
module.exports = {
  async list({ navigation_tab, keyword, page = 1, pageSize = 20 }) {
    const permission = require('permission')
    const filter = await permission.getUserFilter(this.getUniIdToken().uid)
    
    const db = uniCloud.database()
    const where = {
      navigation_tab,
      is_active: true,
      ...filter
    }
    
    if (keyword) {
      where.$or = [
        { name: new RegExp(keyword, 'i') },
        { description: new RegExp(keyword, 'i') }
      ]
    }
    
    const countRes = await db.collection('agents').where(where).count()
    const listRes = await db.collection('agents')
      .where(where)
      .skip((page - 1) * pageSize)
      .limit(pageSize)
      .orderBy('sort_order', 'asc')
      .get()
    
    return {
      code: 0,
      data: {
        list: listRes.data,
        total: countRes.total,
        page,
        pageSize
      }
    }
  }
}
```

#### 6-2. AI 聊天页面 (pages/chat/chat.vue)
- [ ] 从 URL 参数获取 `agent_id`
- [ ] 调用 `agent.detail({ id })` 获取智能体信息
- [ ] 显示聊天界面:
  - 顶部: 智能体名称 + 返回按钮
  - 中间: 消息列表 (用户消息 + AI 回复)
  - 底部: 输入框 + 发送按钮
- [ ] 调用 `chat.sendMessage({ agent_id, message })` 发送消息
- [ ] 支持流式返回 (WebSocket 或 SSE,如果 uniCloud 支持)
- [ ] 保存聊天历史到 `chat-history` 集合

**云对象开发**:
```javascript
// chat/index.obj.js
module.exports = {
  async sendMessage({ agent_id, message, conversation_id }) {
    // 1. 获取智能体配置
    const db = uniCloud.database()
    const agent = await db.collection('agents').doc(agent_id).get()
    
    // 2. 调用 AI API (OpenAI / 火山 / 其他)
    const aiResponse = await callAIAPI({
      system_prompt: agent.data[0].system_prompt,
      user_message: message,
      model: agent.data[0].model_id,
      temperature: agent.data[0].temperature,
      max_tokens: agent.data[0].max_tokens
    })
    
    // 3. 保存聊天历史
    await db.collection('chat-history').add({
      user_id: this.getUniIdToken().uid,
      agent_id,
      conversation_id,
      user_message: message,
      ai_response: aiResponse,
      created_at: Date.now()
    })
    
    return { code: 0, data: { response: aiResponse } }
  }
}
```

---

### Week 9-10: 服务页面 (网页卡片)

#### 9-1. 网页卡片列表页 (pages/service/service.vue)
- [ ] 调用 `web-card.list({ navigation_tab: '服务' })` 获取数据
- [ ] 权限过滤 (自动按城市/部门)
- [ ] 卡片展示:
  - 图标 (Lucide 图标 或 自定义图标URL)
  - 标题 + 描述
  - URL 预览
- [ ] 点击卡片:
  - 如果 `open_mode === 'iframe'`: 在 WebView 内打开
  - 如果 `open_mode === 'external'`: 在外部浏览器打开
  - 如果 `open_mode === '_blank'`: 新标签页打开
- [ ] 搜索 + 分类筛选

**云对象优化** (类似 agent.list,加上权限过滤)

---

### Week 11-12: 飞书数据页面 + 学习中心

#### 11-1. 飞书卡片列表页 (pages/learning/learning.vue 或新建)
- [ ] 调用 `feishu.listCards()` 获取飞书卡片配置
- [ ] 权限过滤 (按城市/部门)
- [ ] 卡片展示 (类似网页卡片)
- [ ] 点击卡片 → 跳转到飞书数据展示页

#### 11-2. 飞书数据展示页 (pages/feishu-data/feishu-data.vue)
- [ ] 调用 `feishu.getData({ card_id, page })` 获取数据
- [ ] 数据列表展示:
  - 表头: 根据 `display_fields` 配置显示列
  - 数据行: 卡片形式展示每条记录
  - 展开/折叠: 显示全部字段 或 折叠字段
- [ ] 基础筛选:
  - 第一期仅支持简单文本搜索
  - 高级筛选 (filter_config) 后期再做
- [ ] 分页加载

**云对象开发**:
```javascript
// feishu/index.obj.js
module.exports = {
  async listCards() {
    const permission = require('permission')
    const filter = await permission.getUserFilter(this.getUniIdToken().uid)
    
    const db = uniCloud.database()
    return await db.collection('feishu-cards')
      .where({ is_active: true, ...filter })
      .get()
  },
  
  async getData({ card_id, page = 1, pageSize = 20, keyword }) {
    // 1. 获取卡片配置
    const db = uniCloud.database()
    const card = await db.collection('feishu-cards').doc(card_id).get()
    const config = card.data[0]
    
    // 2. 调用飞书 API 获取数据
    const feishuSDK = require('feishu-sdk')
    const data = await feishuSDK.getTableData({
      app_id: config.app_id,
      app_secret: config.app_secret,
      table_url: config.table_url,
      page,
      pageSize,
      keyword
    })
    
    return { code: 0, data }
  }
}
```

#### 11-3. 学习中心页面 (可选,或与飞书合并)
- [ ] 如果有独立学习资源,单独做一个页面
- [ ] 否则复用飞书数据页面

---

### Week 12: 个人中心 + 测试优化

#### 12-1. 个人中心页 (pages/mine/mine.vue)
- [ ] 用户信息展示:
  - 头像 + 姓名
  - 手机号 / 邮箱
  - 城市 / 部门
- [ ] 功能入口:
  - 编辑资料 (头像、昵称)
  - 修改密码
  - 退出登录
- [ ] (可选) 显示管理入口按钮:
  - 仅当 `is_admin === true` 时显示
  - 点击跳转到 `/pages-admin/admin-index/admin-index`
  - 但此时管理端还没开发,可以先隐藏

#### 12-2. 客户端测试与优化
- [ ] 功能测试:
  - 登录/注册/退出
  - 智能体列表 + 聊天
  - 网页卡片打开
  - 飞书数据展示
- [ ] 权限测试:
  - 创建不同城市/部门的测试用户
  - 验证只能看到授权的智能体/卡片
- [ ] 性能优化:
  - 列表分页加载
  - 图片懒加载
  - 缓存常用数据
- [ ] UI/UX 优化:
  - 加载状态
  - 空状态提示
  - 错误提示优化

---

## 🎉 阶段 1 完成标志

**客户端 MVP 可用**:
- ✅ 用户可以登录并使用智能体对话
- ✅ 用户可以访问网页卡片和飞书数据
- ✅ 权限控制正常 (不同城市/部门看到不同内容)
- ✅ 基础功能稳定,无重大 Bug

**暂时通过手动方式管理**:
- 📝 智能体/网页卡片/飞书卡片: 通过 uniCloud Web 控制台手动添加
- 📝 用户管理: 通过数据库操作
- 📝 城市/部门管理: 通过数据库操作

---

## 📅 阶段 2: 管理端开发 (Week 13-24, 约 3 个月)

> 此时客户端已经可以正常使用,管理端开发不会影响用户

### Week 13-14: 管理端基础架构

#### 13-1. 创建管理端目录结构
- [ ] 在 `pages.json` 中配置 `subPackages`:
  ```json
  {
    "subPackages": [{
      "root": "pages-admin",
      "pages": [
        { "path": "admin-index/admin-index" },
        { "path": "users/users" },
        { "path": "cities/cities" },
        { "path": "departments/departments" },
        { "path": "agents/agents" },
        { "path": "web-cards/web-cards" },
        { "path": "feishu-cards/feishu-cards" },
        { "path": "card-sort/card-sort" }
      ]
    }]
  }
  ```
- [ ] 创建空页面文件

#### 13-2. 开发 admin 云对象
- [ ] 创建 `uniCloud/cloudfunctions/admin/index.obj.js`
- [ ] 实现 `_before()` 统一鉴权
- [ ] 实现基础方法:
  - `listUsers()` / `updateUser()` / `resetPassword()`
  - `listCities()` / `createCity()` / `updateCity()`
  - `listDepartments()` / `createDepartment()` / `updateDepartment()`
  - `listAgents()` / `createAgent()` / `updateAgent()` / `deleteAgent()`
  - `listWebCards()` / `createWebCard()` / `updateWebCard()` / `deleteWebCard()`
  - `listFeishuCards()` / `createFeishuCard()` / `updateFeishuCard()` / `deleteFeishuCard()`
  - `updateCardOrder()`

#### 13-3. 集成 uView UI
- [ ] 安装 uView UI: `npm install uview-ui@2.x`
- [ ] 在 `main.js` 中引入
- [ ] 配置主题色 (紫色 #667eea)

#### 13-4. 管理端入口
- [ ] 在 `pages/mine/mine.vue` 中添加"管理面板"按钮
- [ ] 仅当 `is_admin === true` 时显示
- [ ] 点击跳转到 `/pages-admin/admin-index/admin-index`

---

### Week 15-16: 管理首页 + 城市部门管理

#### 15-1. 管理首页 (pages-admin/admin-index/admin-index.vue)
- [ ] 顶部: 显示管理员信息 (系统管理员 / X城市管理员)
- [ ] 网格卡片展示管理模块:
  - 用户管理 / 城市管理 / 部门管理
  - 智能体管理 / 网页卡片管理 / 飞书卡片管理
  - 卡片排序
- [ ] 根据角色动态显示 (城市管理员隐藏部分模块)

#### 15-2. 城市管理页 (pages-admin/cities/cities.vue)
- [ ] 使用 uView UI 表格组件 (H5 端)
- [ ] 列表展示: 城市名、编码、排序、启用状态
- [ ] 操作: 新增、编辑、启用/禁用、排序
- [ ] 仅系统管理员可访问

#### 15-3. 部门管理页 (pages-admin/departments/departments.vue)
- [ ] 类似城市管理
- [ ] 列表展示: 部门名、描述、排序、启用状态

---

### Week 17-18: 用户管理

#### 17-1. 用户管理页 (pages-admin/users/users.vue)
- [ ] 列表展示: 用户名、手机号、城市、部门、角色、状态
- [ ] 顶部筛选: 城市、部门、关键词搜索
- [ ] 操作: 编辑、启用/禁用、重置密码
- [ ] 权限控制:
  - 系统管理员: 可管理所有用户
  - 城市管理员: 仅能管理本城市用户

#### 17-2. 用户编辑弹窗
- [ ] 表单字段: 城市、部门、角色、管理员标记、启用状态
- [ ] 提交调用 `admin.updateUser()`

---

### Week 19-20: 智能体 + 网页卡片管理

#### 19-1. 智能体管理页 (pages-admin/agents/agents.vue)
- [ ] 列表展示: 名称、导航标签、城市范围、部门范围、启用状态
- [ ] 操作: 新增、编辑、删除、排序
- [ ] 编辑表单:
  - 名称、描述、导航标签
  - system_prompt (多行文本)
  - 模型配置 (model_id, temperature, max_tokens)
  - 图标配置 (icon_name, icon_color)
  - 权限配置 (城市、部门 多选)
  - 启用状态

#### 19-2. 网页卡片管理页 (pages-admin/web-cards/web-cards.vue)
- [ ] 类似智能体管理
- [ ] 编辑表单:
  - 标题、描述、URL
  - 打开方式 (iframe / external / _blank)
  - 图标配置
  - 导航标签、权限配置、启用状态

---

### Week 21-22: 飞书卡片管理 + 卡片排序

#### 21-1. 飞书卡片管理页 (pages-admin/feishu-cards/feishu-cards.vue)
- [ ] 列表展示: 标题、表URL、导航标签、权限、状态
- [ ] 操作: 新增、编辑、删除、验证配置
- [ ] 编辑表单 (第一期简化版):
  - 标题、描述
  - App ID、App Secret、表 URL
  - 导航标签、权限配置、启用状态
- [ ] "验证配置"按钮: 测试飞书 API 连通性

#### 21-2. 卡片排序页 (pages-admin/card-sort/card-sort.vue)
- [ ] 选择: 卡片类型 (智能体/网页/飞书)、导航标签、城市、部门
- [ ] 显示当前卡片列表 (按 sort_order 排序)
- [ ] 提供上移/下移按钮
- [ ] 保存排序: 调用 `admin.updateCardOrder()`

---

### Week 23-24: 优化测试与发布

#### 23-1. H5 大屏优化
- [ ] 优化表格展示 (1920x1080 / 1366x768)
- [ ] 优化筛选器布局
- [ ] 快捷键支持 (Ctrl+S 保存)

#### 23-2. 移动端适配 (简化版)
- [ ] 用卡片列表代替表格
- [ ] 简化操作 (仅保留启用/禁用、简单编辑)

#### 23-3. 功能测试
- [ ] 权限测试 (系统管理员 vs 城市管理员)
- [ ] CRUD 测试 (所有管理功能)
- [ ] 性能测试 (1000+ 条数据)

#### 23-4. 灰度发布
- [ ] 先给 1-2 位管理员试用
- [ ] 收集反馈并修复
- [ ] 正式上线

---

## 🎉 阶段 2 完成标志

**管理端可用**:
- ✅ 管理员可以通过 H5 管理界面管理所有数据
- ✅ 权限控制正常 (系统管理员 vs 城市管理员)
- ✅ 基础 CRUD 功能完整
- ✅ 不再需要手动操作数据库

---

## 📊 时间线总结

```
Month 1 (Week 1-4)   : 公共基础 + 工作台 + 智能体列表
Month 2 (Week 5-8)   : AI聊天 + 服务页面 + 飞书数据
Month 3 (Week 9-12)  : 学习中心 + 个人中心 + 测试优化
--------------------------- 客户端 MVP 完成 ---------------------------
Month 4 (Week 13-16) : 管理端架构 + 城市部门 + 用户管理
Month 5 (Week 17-20) : 智能体管理 + 网页卡片管理
Month 6 (Week 21-24) : 飞书卡片管理 + 卡片排序 + 测试发布
--------------------------- 管理端完成 ---------------------------
```

---

## 🚧 阶段 1 临时运营方案 (Week 1-12)

### 如何手动管理数据?

**通过 uniCloud Web 控制台**:

1. **添加智能体**:
   - 打开 uniCloud Web 控制台 → 云数据库 → `agents` 集合
   - 点击"新增文档"
   - 填写 JSON:
     ```json
     {
       "name": "教学助手",
       "description": "帮助老师备课",
       "system_prompt": "你是一个...",
       "model_id": "gpt-3.5-turbo",
       "temperature": 0.7,
       "max_tokens": 2000,
       "navigation_tab": "教研",
       "cities": ["all"],
       "departments": ["all"],
       "icon_name": "BookOpen",
       "icon_color": "#667eea",
       "is_active": true,
       "sort_order": 1,
       "created_at": 1699999999
     }
     ```

2. **添加网页卡片**:
   - 类似智能体,在 `web-cards` 集合中新增文档

3. **添加飞书卡片**:
   - 在 `feishu-cards` 集合中新增文档

4. **用户管理**:
   - 修改用户的 `city`, `department`, `is_admin` 等字段

5. **城市/部门数据**:
   - 在 `cities` 和 `departments` 集合中提前添加好数据

---

## 🎯 关键成功因素

### 单人开发的 3 个建议

1. **不要追求完美,先做能用的**
   - 客户端第一期可以砍掉一些高级功能
   - 飞书高级筛选、折叠字段配置可以后置
   - 管理端移动端适配可以简化

2. **做好代码复用**
   - 卡片展示组件统一封装
   - 列表分页逻辑统一封装
   - 表单验证逻辑统一封装

3. **定期休息和总结**
   - 每完成一个大模块 (如智能体) 就休息 1-2 天
   - 每月底回顾进度,调整计划
   - 不要连续加班,保持可持续开发

---

## 📝 每周检查清单

**Week 1-12 (客户端开发期)**:
- [ ] 每周五下午: 回顾本周完成的任务
- [ ] 每周日晚上: 规划下周要做的事
- [ ] 每 2 周: 在测试设备上运行一次,体验用户视角

**Week 13-24 (管理端开发期)**:
- [ ] 每周五: 用管理端操作一遍,看是否比控制台方便
- [ ] 每 2 周: 让其他人 (如果有) 试用管理端

---

**文档版本**: v1.0  
**适用场景**: 单人开发,客户端优先  
**预计完成时间**: 6 个月
