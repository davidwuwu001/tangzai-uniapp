# 汤仔助手 - 路由配置说明

## 📋 当前路由结构

### TabBar页面 (5个)

| 序号 | 页面名称 | 路径 | Tab文字 | 状态 |
|------|---------|------|---------|------|
| 1 | 工作台 | `/pages/workbench/workbench` | 工作台 | ✅ 已完成 |
| 2 | 智能体列表 | `/pages/teaching/teaching` | 智能体 | ⏳ 待开发 |
| 3 | 我的服务 | `/pages/service/service` | 我的服务 | ⏳ 待开发 |
| 4 | 学习中心 | `/pages/learning/learning` | 学习中心 | ⏳ 待开发 |
| 5 | 我的 | `/pages/mine/mine` | 我的 | ⏳ 待开发 |

### 其他页面

| 页面名称 | 路径 | 导航栏 | 状态 |
|---------|------|--------|------|
| 登录页 | `/pages/login/login` | 自定义 | ✅ 已完成 |
| 注册页 | `/pages/register/register` | 自定义 | ✅ 已完成 |
| AI对话页 | `/pages/chat/chat` | 标准 | ⏳ 待开发 |
| 飞书数据列表 | `/pages/feishu/list` | 标准 | ⏳ 待开发 |
| 飞书详情页 | `/pages/feishu/detail` | 标准 | ⏳ 待开发 |
| 咨询日历 | `/pages/calendar/calendar` | 标准 | ⏳ 待开发 |
| 对话历史 | `/pages/chat/history` | 标准 | ⏳ 待开发 |
| 设置页 | `/pages/settings/settings` | 标准 | ⏳ 待开发 |

## 🎯 首页配置

**当前首页**: `/pages/workbench/workbench` (工作台)

### 登录后跳转规则

所有登录成功后的跳转,**必须跳转到工作台**:

```javascript
// ✅ 正确写法
uni.switchTab({
  url: '/pages/workbench/workbench'
})

// ❌ 错误写法 (旧代码)
uni.switchTab({
  url: '/pages/teaching/teaching'
})
```

### 登录跳转位置清单

1. **登录页** (`pages/login/login.vue`)
   - 第197行: `checkAutoLogin()` 自动登录跳转
   - 第261行: `handleLogin()` 登录成功跳转
   - ✅ 已修改为工作台

2. **注册页** (`pages/register/register.vue`)
   - 注册成功后跳转
   - ⚠️ 需要检查

## 📝 新增页面规范

### 1. 在 pages.json 中添加页面配置

```json
{
  "path": "pages/新页面目录/新页面名称",
  "style": {
    "navigationBarTitleText": "页面标题",
    "navigationStyle": "default" // 或 "custom"
  }
}
```

### 2. 如果是TabBar页面

在 `tabBar.list` 中添加:

```json
{
  "pagePath": "pages/新页面目录/新页面名称",
  "text": "Tab文字"
}
```

### 3. 页面跳转方式

- **TabBar页面**: 使用 `uni.switchTab()`
- **普通页面**: 使用 `uni.navigateTo()`
- **重定向**: 使用 `uni.redirectTo()`
- **返回**: 使用 `uni.navigateBack()`

## 🚀 快捷入口映射

工作台快捷入口对应的路径:

| 快捷入口 | 对应页面 | 路径 |
|---------|---------|------|
| AI智能体 | 智能体列表 | `/pages/teaching/teaching` |
| 咨询安排 | 咨询日历 | `/pages/calendar/calendar` |
| 服务打卡 | 服务打卡 | `/pages/checkin/checkin` |
| 信息录入 | 信息录入 | `/pages/input/input` |
| 学习资料 | 学习资料 | `/pages/learning/learning` |
| 全部功能 | 全部功能 | `/pages/all/all` |

## ⚠️ 注意事项

1. **首页永远是工作台**: 所有登录、注册成功后都跳转到 `/pages/workbench/workbench`
2. **TabBar页面必须用 switchTab**: 不能用 navigateTo
3. **路径必须以 / 开头**: 如 `/pages/xxx/xxx`
4. **新增页面后重新编译**: 修改 pages.json 后需要重新运行项目
5. **自定义导航栏**: 使用 `"navigationStyle": "custom"` 时需要自己实现顶部导航

## 📌 开发检查清单

每次新增页面时检查:

- [ ] pages.json 中已添加页面配置
- [ ] 如果是TabBar页面,已添加到 tabBar.list
- [ ] 页面文件已创建 (xxx.vue)
- [ ] 如果有跳转到首页的逻辑,确保跳转到工作台
- [ ] 重新编译运行,验证路由正确

---

**更新时间**: 2025-11-15  
**维护人员**: 开发团队
