# 汤仔助手 uniapp 架构方案 - 角色驱动设计

## 📊 业务模型总结

### 用户角色（3类）
```
1. 👨‍💼 顾问（80% 用户）- 一线工作者
   - 核心功能：AI智能体、咨询安排、服务打卡
   - 信息接收：通知、学习资料
   - 特殊角色：部分顾问是部门负责人（需要访问部门功能）

2. 🏢 城市部门（15% 用户）- 城市运营
   - 10个城市，各有：教研部、财务部、法务部、顾客部等
   - 功能：城市级运营、数据管理
   - 数据：只看本城市数据

3. 🏛️ 平台部门（5% 用户）- 总部管理
   - 角色：平台各部门负责人
   - 功能：数据汇总、跨城市管理
   - 数据：全国数据 + 城市对比
```

### 权限模型
```
数据隔离：
- 顾问：只看自己的数据 + 本城市通知
- 城市部门：只看本城市数据
- 平台部门：看全国数据

角色叠加：
- 顾问 + 部门负责人 = 顾问功能 + 部门管理功能
- 需要角色切换器
```

---

## 🎯 核心架构方案：自适应TabBar + 角色路由

### 一、TabBar 结构（根据角色动态显示）

#### 👨‍💼 顾问端（普通顾问）
```
TabBar: 4个Tab
├── 🏠 工作台
├── 🤖 智能体
├── 📅 我的服务
└── 👤 我的
```

#### 👨‍💼👔 顾问端（部门负责人）
```
TabBar: 5个Tab
├── 🏠 工作台
├── 🤖 智能体
├── 📅 我的服务
├── 🏢 部门管理  ← 额外Tab
└── 👤 我的
```

#### 🏢 城市部门端
```
TabBar: 4个Tab
├── 🏠 工作台
├── 🏢 部门中心
├── 📊 数据报表
└── 👤 我的
```

#### 🏛️ 平台端
```
TabBar: 4个Tab
├── 📈 数据中心
├── 🏙️ 城市管理
├── 🏢 部门管理
└── 👤 我的
```

---

## 📱 页面详细设计

### 🏠 工作台（顾问端）
```vue
<template>
  <!-- 问候语 -->
  <UserGreeting :username="user.name" />
  
  <!-- 今日待办 -->
  <TodoSection>
    <TodoItem type="consultation" :count="todayConsultations" />
    <TodoItem type="checkin" :count="pendingCheckins" />
  </TodoSection>
  
  <!-- 快捷入口（宫格） -->
  <QuickAccess :items="[
    { id: 1, name: 'AI助手', icon: '🤖', route: '/pages/ai/chat' },
    { id: 2, name: '咨询安排', icon: '📅', route: '/pages/service/consultation' },
    { id: 3, name: '服务打卡', icon: '✅', route: '/pages/service/checkin' },
    { id: 4, name: '学习资料', icon: '📚', route: '/pages/learning/materials' }
  ]" />
  
  <!-- 最新通知（3条） -->
  <NoticeBoard :notices="latestNotices" />
  
  <!-- 本月数据概览 -->
  <MonthlyStats :data="{
    consultations: 28,
    checkins: 25,
    studyHours: 12
  }" />
</template>
```

### 🤖 智能体（顾问端）
```vue
<template>
  <!-- 搜索框 -->
  <SearchBar placeholder="搜索智能体..." />
  
  <!-- 常用智能体（横向滚动） -->
  <FavoriteAgents :agents="favoriteAgents" />
  
  <!-- 分类标签 -->
  <CategoryTabs v-model="activeCategory" :tabs="[
    { id: 'all', name: '全部' },
    { id: 'teaching', name: '教研' },
    { id: 'service', name: '服务' },
    { id: 'management', name: '管理' }
  ]" />
  
  <!-- 智能体列表（网格） -->
  <CardGrid :columns="2">
    <UniversalCard 
      v-for="agent in filteredAgents"
      :card="agent"
      displayMode="grid"
      @click="openChat(agent)"
    />
  </CardGrid>
</template>
```

### 📅 我的服务（顾问端）
```vue
<template>
  <!-- 顶部Tab -->
  <Tabs v-model="activeTab" :tabs="[
    { id: 'consultation', name: '咨询安排' },
    { id: 'checkin', name: '服务打卡' }
  ]" />
  
  <!-- 咨询安排 -->
  <view v-if="activeTab === 'consultation'">
    <!-- 月份选择器 -->
    <MonthPicker v-model="selectedMonth" />
    
    <!-- 统计卡片 -->
    <StatsCard :data="{
      total: 28,
      completed: 25,
      pending: 3
    }" />
    
    <!-- 日历视图（参考原项目 ConsultationCalendar） -->
    <ConsultationCalendar 
      :data="consultationData"
      @dayClick="handleDayClick"
    />
    
    <!-- 列表视图 -->
    <ConsultationList 
      :items="consultations"
      @itemClick="viewDetail"
    />
    
    <!-- 添加按钮（悬浮） -->
    <FloatButton icon="+" @click="addConsultation" />
  </view>
  
  <!-- 服务打卡 -->
  <view v-else>
    <!-- 打卡按钮 -->
    <CheckinButton 
      :enabled="canCheckin"
      @click="doCheckin"
    />
    
    <!-- 今日打卡记录 -->
    <TodayCheckins :items="todayCheckins" />
    
    <!-- 本月打卡记录 -->
    <MonthlyCheckins :items="monthlyCheckins" />
  </view>
</template>
```

### 🏢 部门管理（部门负责人）
```vue
<template>
  <!-- 部门信息 -->
  <DepartmentHeader :department="user.department" />
  
  <!-- 功能分类 -->
  <view class="dept-functions">
    <!-- 人员管理 -->
    <Section title="人员管理">
      <FunctionCard name="成员列表" icon="👥" @click="goTo('/pages/dept/members')" />
      <FunctionCard name="权限设置" icon="🔐" @click="goTo('/pages/dept/permissions')" />
    </Section>
    
    <!-- 数据管理 -->
    <Section title="数据管理">
      <FunctionCard name="部门数据" icon="📊" @click="goTo('/pages/dept/reports')" />
      <FunctionCard name="导出报表" icon="📥" @click="exportData" />
    </Section>
    
    <!-- 通知管理 -->
    <Section title="通知管理">
      <FunctionCard name="发布通知" icon="📢" @click="goTo('/pages/dept/notice')" />
      <FunctionCard name="学习资料" icon="📚" @click="goTo('/pages/dept/materials')" />
    </Section>
  </view>
</template>
```

### 🏠 工作台（城市部门端）
```vue
<template>
  <!-- 城市信息 -->
  <CityHeader :city="user.city" :department="user.department" />
  
  <!-- 数据概览（卡片） -->
  <DataOverview :data="{
    advisors: 45,
    consultations: 320,
    revenue: 158000
  }" />
  
  <!-- 待办事项 -->
  <TodoList :items="todos" />
  
  <!-- 快捷入口 -->
  <QuickAccess :items="deptQuickAccess" />
  
  <!-- 最新动态 -->
  <ActivityFeed :activities="recentActivities" />
</template>
```

### 🏢 部门中心（城市部门端）
```vue
<template>
  <!-- 顶部部门切换器 -->
  <DepartmentSwitcher 
    v-model="activeDept"
    :departments="cityDepartments"
  />
  
  <!-- 当前部门功能 -->
  <view class="dept-content">
    <!-- 教研部功能 -->
    <view v-if="activeDept === 'teaching'">
      <FunctionGrid :items="teachingFunctions" />
    </view>
    
    <!-- 财务部功能 -->
    <view v-else-if="activeDept === 'finance'">
      <FunctionGrid :items="financeFunctions" />
    </view>
    
    <!-- ... 其他部门 -->
  </view>
</template>
```

### 📈 数据中心（平台端）
```vue
<template>
  <!-- 全国数据汇总 -->
  <NationalStats :data="nationalData" />
  
  <!-- 城市排行榜 -->
  <CityRanking :cities="cityRanking" />
  
  <!-- 趋势图表 -->
  <TrendCharts :data="trendData" />
  
  <!-- 关键指标 -->
  <KPICards :kpis="keyKPIs" />
</template>
```

### 🏙️ 城市管理（平台端）
```vue
<template>
  <!-- 城市列表（左侧侧边栏） -->
  <CityList 
    v-model="selectedCity"
    :cities="allCities"
  />
  
  <!-- 城市详情（右侧主区域） -->
  <CityDetail :city="selectedCity">
    <!-- 城市数据 -->
    <CityStats :data="cityData" />
    
    <!-- 部门状态 -->
    <DepartmentStatus :departments="cityDepartments" />
    
    <!-- 操作按钮 -->
    <ActionButtons>
      <button>查看报表</button>
      <button>导出数据</button>
      <button>发送通知</button>
    </ActionButtons>
  </CityDetail>
</template>
```

---

## 📁 目录结构设计

```
test1113/
├── pages/
│   ├── auth/                    # 认证相关
│   │   ├── login/              # 登录
│   │   └── register/           # 注册
│   │
│   ├── advisor/                # 顾问端（核心）
│   │   ├── workbench/         # 工作台
│   │   ├── ai/                # AI智能体
│   │   │   ├── chat/          # 对话页
│   │   │   └── list/          # 智能体列表
│   │   ├── service/           # 我的服务
│   │   │   ├── consultation/  # 咨询安排
│   │   │   │   ├── calendar/ # 日历视图
│   │   │   │   ├── detail/   # 详情页
│   │   │   │   └── form/     # 添加/编辑
│   │   │   └── checkin/      # 服务打卡
│   │   ├── learning/          # 学习中心
│   │   │   ├── materials/    # 学习资料
│   │   │   └── courses/      # 培训课程
│   │   └── mine/              # 我的
│   │
│   ├── department/             # 部门功能（顾问负责人/城市部门共用）
│   │   ├── overview/          # 部门概览
│   │   ├── members/           # 成员管理
│   │   ├── reports/           # 数据报表
│   │   ├── notice/            # 通知管理
│   │   └── materials/         # 资料管理
│   │
│   ├── city/                  # 城市部门端
│   │   ├── workbench/         # 工作台
│   │   ├── center/            # 部门中心
│   │   └── reports/           # 数据报表
│   │
│   ├── platform/              # 平台端
│   │   ├── dashboard/         # 数据中心
│   │   ├── cities/            # 城市管理
│   │   └── departments/       # 部门管理
│   │
│   └── common/                # 通用页面
│       ├── notice/            # 通知详情
│       ├── profile/           # 个人资料
│       └── settings/          # 设置
│
├── components/                 # 组件库
│   ├── business/              # 业务组件
│   │   ├── ConsultationCalendar/  # 咨询日历
│   │   ├── CheckinButton/         # 打卡按钮
│   │   ├── TodoSection/           # 待办事项
│   │   ├── NoticeBoard/           # 通知公告
│   │   ├── DepartmentSwitcher/    # 部门切换器
│   │   ├── CitySelector/          # 城市选择器
│   │   └── DataChart/             # 数据图表
│   │
│   ├── layout/                # 布局组件
│   │   ├── UniversalCard/     # 通用卡片
│   │   ├── CardGrid/          # 网格布局
│   │   ├── Section/           # 分组区块
│   │   └── FloatButton/       # 悬浮按钮
│   │
│   └── base/                  # 基础组件
│       ├── SearchBar/         # 搜索框
│       ├── Tabs/              # 标签页
│       ├── MonthPicker/       # 月份选择器
│       └── LoadingState/      # 加载状态
│
├── api/                       # API服务
│   ├── auth.js               # 认证
│   ├── advisor.js            # 顾问相关
│   ├── consultation.js       # 咨询安排
│   ├── checkin.js            # 打卡
│   ├── agent.js              # 智能体
│   ├── department.js         # 部门管理
│   ├── city.js               # 城市管理
│   ├── platform.js           # 平台管理
│   ├── notice.js             # 通知
│   └── learning.js           # 学习资料
│
├── stores/                    # 状态管理（Pinia）
│   ├── user.js               # 用户状态
│   ├── consultation.js       # 咨询数据
│   ├── department.js         # 部门数据
│   └── notice.js             # 通知数据
│
├── utils/                     # 工具函数
│   ├── request.js            # 网络请求
│   ├── cache.js              # 数据缓存
│   ├── config.js             # 配置
│   ├── permission.js         # 权限控制
│   ├── role.js               # 角色判断
│   └── date.js               # 日期处理
│
└── static/                    # 静态资源
    ├── images/
    └── icons/
```

---

## 🔐 权限控制方案

### 1. 用户数据结构
```javascript
// 登录后后端返回
const userProfile = {
  id: 1,
  username: '张三',
  name: '张三',
  
  // 角色信息
  role: 'advisor',  // advisor | city_dept | platform_dept
  subRole: 'dept_leader',  // 子角色：dept_leader（部门负责人）
  
  // 组织信息
  city: '北京',
  department: '教研部',
  
  // 权限列表
  permissions: [
    'consultation.view',
    'consultation.create',
    'checkin.view',
    'dept.manage_members',  // 部门负责人权限
    'dept.publish_notice'   // 部门负责人权限
  ]
}
```

### 2. 角色判断工具
```javascript
// utils/role.js
export const RoleChecker = {
  // 是否是顾问
  isAdvisor(user) {
    return user.role === 'advisor'
  },
  
  // 是否是部门负责人
  isDeptLeader(user) {
    return user.subRole === 'dept_leader'
  },
  
  // 是否是城市部门
  isCityDept(user) {
    return user.role === 'city_dept'
  },
  
  // 是否是平台部门
  isPlatformDept(user) {
    return user.role === 'platform_dept'
  },
  
  // 获取TabBar配置
  getTabBarConfig(user) {
    if (this.isAdvisor(user)) {
      if (this.isDeptLeader(user)) {
        // 顾问 + 部门负责人
        return ['workbench', 'ai', 'service', 'department', 'mine']
      } else {
        // 普通顾问
        return ['workbench', 'ai', 'service', 'mine']
      }
    } else if (this.isCityDept(user)) {
      return ['workbench', 'center', 'reports', 'mine']
    } else if (this.isPlatformDept(user)) {
      return ['dashboard', 'cities', 'departments', 'mine']
    }
    return []
  }
}
```

### 3. 页面权限守卫
```javascript
// utils/permission.js
export function checkPagePermission(user, pagePath) {
  // 部门管理页面：只有部门负责人、城市部门、平台部门可访问
  if (pagePath.startsWith('/pages/department/')) {
    return RoleChecker.isDeptLeader(user) || 
           RoleChecker.isCityDept(user) || 
           RoleChecker.isPlatformDept(user)
  }
  
  // 城市管理页面：只有平台部门可访问
  if (pagePath.startsWith('/pages/platform/')) {
    return RoleChecker.isPlatformDept(user)
  }
  
  return true
}
```

---

## 🚀 实施计划（分4阶段）

### 🏃 第一阶段（Week 1-2）：顾问端核心功能
- [x] Day 1-6：基础框架（已完成）
- [ ] Day 7-10：工作台 + AI智能体列表
- [ ] Day 11-14：咨询安排（日历视图 + 列表）

**交付物：** 顾问可以登录、使用AI、查看咨询安排

---

### 🚴 第二阶段（Week 3-4）：顾问端完善 + 部门负责人
- [ ] Day 15-20：服务打卡 + 学习中心
- [ ] Day 21-25：部门管理页面（负责人）
- [ ] Day 26-28：通知系统 + 个人中心

**交付物：** 顾问端完整功能 + 部门负责人可管理部门

---

### 🏊 第三阶段（Week 5-6）：城市部门端
- [ ] Day 29-35：城市部门工作台 + 部门中心
- [ ] Day 36-42：数据报表 + 部门功能

**交付物：** 城市部门可独立运营管理

---

### 🚀 第四阶段（Week 7-8）：平台端 + 优化
- [ ] Day 43-49：平台数据中心 + 城市管理
- [ ] Day 50-56：平台部门管理 + 权限系统
- [ ] Day 57-60：性能优化 + 测试 + 打包

**交付物：** 完整系统 + APP安装包

---

## 💡 关键技术点

### 1. 动态TabBar
```javascript
// App.vue 或 main.js
import { RoleChecker } from '@/utils/role'

onLaunch() {
  const user = getUserInfo()
  const tabBarConfig = RoleChecker.getTabBarConfig(user)
  
  // 动态配置TabBar
  uni.setTabBarList(tabBarConfig)
}
```

### 2. 咨询日历组件（复用原项目逻辑）
```javascript
// 参考 tangzai-zhushou-1105/components/ConsultationCalendar
// 核心功能：
// - 月份视图
// - 日期点击
// - 显示咨询数量
// - 标记已完成/未完成
```

### 3. 数据缓存策略
```javascript
// 按角色 + 城市 缓存数据
const cacheKey = `${role}_${city}_${dataType}`

// 顾问数据：缓存5分钟
// 部门数据：缓存10分钟
// 平台数据：缓存15分钟
```

---

## ❓ 待确认问题

1. **咨询安排表单**：除了服务人员、服务时间、学生姓名，还有哪些必填字段？
2. **服务打卡**：打卡时需要填写什么信息？（服务内容、服务时长？）
3. **学习资料**：格式是什么？（PDF、视频、文章？）
4. **通知分类**：系统通知 vs 部门通知，显示方式是否不同？
5. **部门负责人**：需要管理哪些具体内容？（人员、数据、通知？）

---

这个方案的核心优势：
✅ 清晰的角色分离
✅ 灵活的权限控制
✅ 可扩展的架构
✅ 复用现有组件逻辑
✅ 分阶段实施，降低风险

你觉得这个方案如何？有什么需要调整的吗？🤔
